<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 指定工作空间，要与接口名相同，通过"这里的namespace.下边方法的id"来定位方法 -->
<mapper namespace="com.example.redis.project.redis.com.yto.cn.mapper.CommonMapper">
    <select id="getWaybillNoMessage" parameterType="java.lang.String" resultType="java.util.List">

    </select>

</mapper>

